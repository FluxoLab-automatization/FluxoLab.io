{
  "name": "fluxolab-platform",
  "version": "1.0.0",
  "description": "Monorepo da plataforma FluxoLab (backend NestJS, frontend Vue e ferramentas de banco).",
  "scripts": {
    "start": "npm run start --prefix backend",
    "start:dev": "npm run start:dev --prefix backend",
    "build": "npm run build --prefix backend",
    "migrate": "node db/migrate.js",
    "test": "npm run test --prefix backend",
    "lint": "npm run lint --prefix backend",
    "cf:build": "npm ci --prefix frontend && npm run build --prefix frontend"
  },
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.12.0",
    "pino": "^9.1.0",
    "pino-pretty": "^11.2.2"
  }
}
